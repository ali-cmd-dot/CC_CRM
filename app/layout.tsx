import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'FleetTrack Pro - Enterprise CRM',
  description: 'Complete Fleet Management System with Real-time Tracking',
  keywords: 'fleet management, CRM, vehicle tracking, employee management',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
